# variables
fNum = input("First number: ")
sNum = input("Second number: ")

# addition
# We are using float to be able to add decimal numbers too
summ = float(fNum) + float(sNum)

print("The sum is " + str(summ))  # displaying
